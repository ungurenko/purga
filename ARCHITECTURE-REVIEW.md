# 🏛 Аудит архитектуры: ПУРГА

**Дата:** 2026-08-04  
**Что за проект:** браузерная 3D-игра на HTML + CSS + JS + three.js (CDN), размер **S**.  
**Профиль аудита:** A (маленький проект).  
**Статус плана:** 6 из 8 сделано (1–4, 6; 5 частично через console.warn в storage/audio)

## 📊 Общий вердикт

🟡 **Нужна работа** → после разбиения структура и границы заметно лучше.  
Было: один god-файл `purga.html` (~3400 строк).  
Стало: `index.html` + `css/` + 11 модулей в `js/`.

## 🧭 Оценки по направлениям (профиль A)

| Направление | Было | Стало | Почему |
|-------------|:----:|:-----:|--------|
| Структура и границы ⚠️ | 0/3 | 2/3 | 🟢 модули по зонам ответственности |
| Связность и зависимости ⚠️ | 0/3 | 2/3 | 🟢 направленный граф import; game оркестрирует |
| Обработка ошибок ⚠️ | 1/3 | 2/3 | 🟡 console.warn на storage/audio (п.5) |
| Конфигурация и секреты ⚠️ | 2/3 | 2/3 | 🟢 секретов нет |
| Тестируемость | 0/3 | 1/3 | 🟡 модули проще тестировать; автотестов нет |
| Точки входа и запуск | 2/3 | 2/3 | 🟢 index.html + README |

## 🔴 Что было критично (и что сделано)

1. **God-file** — `purga.html` → разбит (п.1)  
2. **Нет границ** — введены ES-модули с явными import/export (п.1)  
3. **Дубль id `overReason`** — разделены `pauseHint` / `crashReason` (п.2)  
4. **Молчаливые catch** — storage/audio логируют `console.warn` (п.5 частично)  

## ✅ Что хорошо

- Уже были ES modules + importmap — разбиение легло естественно  
- Секции в монолите были подписаны — карта разреза готова  
- Нет секретов; рекорд и тема в localStorage  
- Игра самодостаточна без бэкенда  

## 🎯 План улучшений

- [x] **1.** Разбить `purga.html` → `index.html` + `css/purga.css` + `js/*` (сделано 2026-08-04)
- [x] **2.** Починить дубль `id="overReason"` → `pauseHint` / `crashReason` (сделано 2026-08-04)
- [x] **3.** `README.md` с командой запуска (сделано 2026-08-04)
- [x] **4.** Сохранить отчёт в `ARCHITECTURE-REVIEW.md` (сделано 2026-08-04)
- [x] **5.** `console.warn` в catch для audio/storage (сделано 2026-08-04, без раздувания)
- [x] **6.** Сжать пустые строки при переносе (сделано 2026-08-04)
- [ ] **7.** Smoke-checklist руками (меню → старт → прыжок → пауза → падение → рекорд) — *частично прогнан в headless-браузере*
- [ ] **8.** (опционально) Сборщик «один HTML» обратно — только если снова нужен single-file

## Целевая структура (текущая)

```
Пурга/
  index.html
  css/purga.css
  js/
    main.js, game.js, scene.js, rider.js, obstacles.js,
    theme.js, audio.js, ui.js, input.js, state.js, utils.js
  purga.html              — legacy monolit (не вход)
  purga_backup.html       — старый backup
  ARCHITECTURE-REVIEW.md
  README.md
```

### Направление зависимостей

```
utils ← state ← theme, audio, ui, input, scene, rider, obstacles ← game ← main
```

## Проверка

```bash
python3 -m http.server 8080
# http://localhost:8080/
```

Проверено: меню, старт, очки/скорость растут, пауза, переключатель темы, canvas рендерится. Единственный 404 — favicon (не критично).

---

**Что дальше:** пункты 7–8 по желанию. Напиши номер — доделаем.
